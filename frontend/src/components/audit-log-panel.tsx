import type { AuditLog } from "@/types";
export function AuditLogPanel({items}:{items:AuditLog[]}){return !items.length?<div className="empty"><h2>No audit records</h2></div>:<div className="dataTableWrap"><table className="dataTable"><thead><tr><th>Action</th><th>Target</th><th>Actor ID</th><th>Details</th><th>Time</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td><strong>{item.action.replaceAll("_"," ")}</strong></td><td>{item.target_type}<div className="muted">{item.target_id??"—"}</div></td><td>{item.actor_id??"System"}</td><td><code>{item.metadata_json?JSON.stringify(item.metadata_json):"—"}</code></td><td>{new Date(item.created_at).toLocaleString("en-BD")}</td></tr>)}</tbody></table></div>}
