import type { Metadata } from "next";
export const metadata:Metadata={title:"How it works"};
const steps=[["1","Create or find a listing","Choose free, discounted or exchange. Public listings show the food, general area and expiry time."],["2","Send a structured request","Orders and exchanges are stored in the database. The provider can accept or reject them."],["3","Collect or deliver safely","Exact pickup details become available only to authorized participants after acceptance."],["4","Confirm completion","Both parties confirm the handover. Only then does the review system unlock."]];
export default function Page(){return <main className="page"><div className="container stack"><div><span className="badge badgeFree">Simple by design</span><h1 className="sectionTitle" style={{marginTop:16}}>From surplus food to a second table.</h1><p className="sectionLead">We Eat separates public discovery from private fulfilment details.</p></div><div className="grid">{steps.map(([n,t,d])=><article className="card" style={{padding:28}} key={n}><div style={{width:48,height:48,borderRadius:16,display:"grid",placeItems:"center",background:"var(--blue-strong)",fontWeight:900}}>{n}</div><h2>{t}</h2><p className="muted" style={{lineHeight:1.7}}>{d}</p></article>)}</div></div></main>}
