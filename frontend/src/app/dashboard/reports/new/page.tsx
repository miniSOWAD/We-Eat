import { FileWarning, ShieldCheck } from "lucide-react";
import { ReportForm } from "@/components/report-form";
export default function Page(){return <div className="stack"><div className="pageIntro"><span className="badge badgeFree">Community safety</span><h1 className="sectionTitle">Submit a report</h1><p className="sectionLead">Send a listing, user or comment to the moderation queue. Reports are not public.</p></div><div className="softPanel"><ShieldCheck size={18} style={{verticalAlign:"-4px",marginRight:7}}/><strong>Use factual details.</strong><span className="muted"> The target ID is visible in the related page URL or can be copied from the relevant item.</span></div><ReportForm/><div className="help"><FileWarning size={15} style={{verticalAlign:"-3px",marginRight:6}}/>For urgent physical danger, contact local emergency services rather than relying only on an in-app report.</div></div>}
